<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@android:color/holo_blue_light</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textAppearanceLargePopupMenu">@style/myPopupMenu</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/custom_action_mode_light</item>
        <item name="android:actionModeStyle">@style/custom_action_mode_light</item>
    </style>

    <style name="list_item_info_style">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>
    
    <style name="myPopupMenu" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:textColor">#3F3F3F</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="custom_action_mode_light" parent="Widget.AppCompat.ActionMode">
        <item name="actionBarPopupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="background">@android:color/holo_blue_light</item>
        <item name="android:background">@android:color/holo_blue_light</item>
    </style>

    <style name="AlertDialogCustom">
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">#6E6E6E</item>
    </style>

</resources>
